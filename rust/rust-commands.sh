######################################################################

rustc --version

rustup --version
rustup update
rustup self uninstall

rustfmt --version

cargo --version
cargo new hello_cargo
cargo build
cargo build --release
cargo run
cargo check
