---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ldap-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard
  # storageClassName: hostpath
  # storageClassName: rook-ceph-block
  # storageClassName: local-path
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ldap-config-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard
  # storageClassName: hostpath
  # storageClassName: rook-ceph-block
  # storageClassName: local-path
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ldap-certs-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard
  # storageClassName: hostpath
  # storageClassName: rook-ceph-block
  # storageClassName: local-path
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: container-run-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard
  # storageClassName: hostpath
  # storageClassName: rook-ceph-block
  # storageClassName: local-path
