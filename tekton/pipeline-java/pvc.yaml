---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: takton-shared-workspace-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard
  # storageClassName: hostpath
  # storageClassName: rook-ceph-block
  # storageClassName: local-path
