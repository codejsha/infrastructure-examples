######################################################################

### show the current configuration
ghostty +show-config
ghostty +show-config --default
ghostty +show-config --default --docs

### validate config fiile
ghostty +validate-config

### preview or list all the available themes
ghostty +list-themes
