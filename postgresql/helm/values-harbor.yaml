postgresqlDatabase: postgres

# postgresqlInitdbArgs:
initdbScripts:
  harbor_initdb_script.sql: |
    CREATE DATABASE registry;
    CREATE DATABASE notary_server;
    CREATE DATABASE notary_signer;
    CREATE DATABASE clair;

# initdbScriptsConfigMap:
# initdbScriptsSecret:

# initdbUser:
# initdbPassword:
