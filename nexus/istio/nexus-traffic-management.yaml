---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: nexus-gw
  namespace: nexus-system
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http-nexus
        protocol: HTTP
      hosts:
        - nexus.example.com
    - port:
        number: 443
        name: https-nexus
        protocol: HTTPS
      hosts:
        - nexus.example.com
      tls:
        mode: PASSTHROUGH
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: nexus-http-vs
  namespace: nexus-system
spec:
  hosts:
    - nexus.example.com
  gateways:
    - nexus-gw
  http:
    - match:
        - port: 80
      route:
        - destination:
            host: my-nexus-nexus-repository-manager.nexus-system.svc.cluster.local
            port:
              number: 8081
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: nexus-https-vs
  namespace: nexus-system
spec:
  hosts:
    - nexus.example.com
  gateways:
    - nexus-gw
  tls:
    - match:
        - port: 443
          sniHosts:
            - nexus.example.com
      route:
        - destination:
            host: my-nexus-nexus-repository-manager.nexus-system.svc.cluster.local
            port:
              number: 8081
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: nexus-dr
  namespace: nexus-system
spec:
  host: my-nexus-nexus-repository-manager.nexus-system.svc.cluster.local
  trafficPolicy:
    tls:
      mode: DISABLE
